<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Demande de Télétravail</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 40px;
            line-height: 1.6;
            color: #333;
        }
        header {
            text-align: center;
            margin-bottom: 40px;
        }
        header h1 {
            color: #007bff;
            font-size: 24px;
            margin: 0;
        }
        header p {
            font-size: 14px;
            color: #555;
        }
        .content {
            border: 1px solid #ddd;
            padding: 20px;
            border-radius: 8px;
            background-color: #f9f9f9;
        }
        .content h2 {
            color: #007bff;
            font-size: 20px;
            margin-bottom: 20px;
        }
        .content p {
            margin: 10px 0;
        }
        .label {
            font-weight: bold;
            color: #555;
        }
        footer {
            text-align: center;
            margin-top: 40px;
            font-size: 12px;
            color: #777;
        }
    </style>
</head>
<body>
    <header>
        <h1>Demande de Télétravail</h1>
        <p>Document généré automatiquement</p>
    </header>
    <div class="content">
        <h2>Détails de la demande</h2>
        <p><span class="label">ID de la demande :</span> {{ teletravail.idTeletravail }}</p>
        <p><span class="label">Employé :</span> {{ teletravail.Employe.nomUser }}</p>
        <p><span class="label">Date de demande :</span> {{ teletravail.dateDemandeTT|date('d/m/Y') }}</p>
        <p><span class="label">Date de début :</span> {{ teletravail.dateDebutTT|date('d/m/Y') }}</p>
        <p><span class="label">Date de fin :</span> {{ teletravail.dateFinTT|date('d/m/Y') }}</p>
        <p><span class="label">Statut :</span> {{ teletravail.statutTT }}</p>
    </div>
    <footer>
        <p>&copy; {{ "now"|date("Y") }} NextGenHR - Tous droits réservés</p>
    </footer>
</body>
</html>