<form method="post" action="{{ path('app_teletravail_delete', {'IdTeletravail': teletravail.IdTeletravail}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ teletravail.IdTeletravail) }}">
    <button class="btn">Delete</button>
</form>
