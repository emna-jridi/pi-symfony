{% extends 'base.html.twig' %}

{% block title %}ReservationSalle{% endblock %}

{% block body %}
    <h1>ReservationSalle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdReservation</th>
                <td>{{ reservation_salle.IdReservation }}</td>
            </tr>
            <tr>
                <th>IdEmploye</th>
                <td>{{ reservation_salle.IdEmploye }}</td>
            </tr>
            <tr>
                <th>IdSalle</th>
                <td>{{ reservation_salle.IdSalle }}</td>
            </tr>
            <tr>
                <th>DateReservation</th>
                <td>{{ reservation_salle.DateReservation ? reservation_salle.DateReservation|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DureeReservation</th>
                <td>{{ reservation_salle.DureeReservation ? reservation_salle.DureeReservation|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>StatutReservation</th>
                <td>{{ reservation_salle.StatutReservation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reservation_salle_index') }}">back to list</a>

    <a href="{{ path('app_reservation_salle_edit', {'IdReservation': reservation_salle.IdReservation}) }}">edit</a>

    {{ include('reservation_salle/_delete_form.html.twig') }}
{% endblock %}
