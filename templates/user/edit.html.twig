{% extends 'baseAdmin.html.twig' %}

{% block title %}Modifier l'employé{% endblock %}

{% block body %}
<div class="container-scroller">
    <!-- partial:partials/_navbar.html -->
    {% include 'partials/_navbar.html.twig' %}
    
    <div class="container-fluid page-body-wrapper">
        <!-- partial:partials/_sidebar.html -->
        {% include 'partials/_sidebarAdmin.html.twig' %}
        
        <!-- partial -->
        <div class="main-panel">
            <div class="content-wrapper">
                <div class="row">
                    <div class="col-lg-12 grid-margin stretch-card">
                        <div class="card">
                            <div class="card-body">
                                <h4 class="card-title">Modifier l'employé</h4>
                                
                                <div class="alert alert-info">
                                    <p>Cette fonctionnalité sera implémentée prochainement.</p>
                                </div>
                                
                                <div class="row mt-4">
                                    <div class="col-md-12">
                                        <a href="{{ path('app_user_index') }}" class="btn btn-secondary">Retour à la liste</a>
                                        <a href="{{ path('app_user_show', {'id': user.idUser}) }}" class="btn btn-info">Voir les détails</a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %} 