{% extends 'base.html.twig' %}

{% block title %}Connexion{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap');
        
        body {
            margin: 0;
            padding: 0;
            font-family: 'Poppins', sans-serif;
            background: #f8f9fa url('/Login/images/backg.png') no-repeat center center fixed;
            background-size: cover;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
        }
        
        .login-container {
            display: flex;
            width: 900px;
            height: 600px;
            background: white;
            border-radius: 15px;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
            overflow: hidden;
        }
        
        .login-image {
            flex: 1;
            background: linear-gradient(135deg, #0e53ab 0%, #0a3d7a 100%);
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 40px;
            position: relative;
            overflow: hidden;
        }
        
        .login-image::before {
            content: '';
            position: absolute;
            width: 200%;
            height: 200%;
            background: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" width="100" height="100" viewBox="0 0 100 100"><rect width="100" height="100" fill="none"/><circle cx="50" cy="50" r="40" stroke="rgba(255,255,255,0.1)" stroke-width="2" fill="none"/></svg>');
            opacity: 0.1;
            animation: rotate 30s linear infinite;
        }
        
        @keyframes rotate {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
        
        .login-image h2 {
            color: white;
            font-size: 28px;
            font-weight: 600;
            margin-bottom: 15px;
            text-align: center;
            position: relative;
            z-index: 1;
        }
        
        .login-image p {
            color: rgba(255, 255, 255, 0.8);
            font-size: 16px;
            line-height: 1.6;
            text-align: center;
            position: relative;
            z-index: 1;
        }
        
        .login-form {
            flex: 1;
            padding: 60px 40px;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }
        
        .login-form h1 {
            color: #0e53ab;
            font-size: 32px;
            font-weight: 700;
            margin-bottom: 10px;
        }
        
        .login-form p {
            color: #6c757d;
            font-size: 14px;
            margin-bottom: 30px;
        }
        
        .form-group {
            position: relative;
            margin-bottom: 25px;
        }
        
        .form-group input {
            width: 100%;
            padding: 15px 15px 15px 45px;
            border: 1px solid #dee2e6;
            border-radius: 8px;
            font-size: 14px;
            color: #495057;
            transition: all 0.3s;
            background: #f8f9fa;
        }
        
        .form-group input:focus {
            border-color: #0e53ab;
            box-shadow: 0 0 0 3px rgba(14, 83, 171, 0.1);
            outline: none;
        }
        
        .form-group i {
            position: absolute;
            left: 15px;
            top: 50%;
            transform: translateY(-50%);
            color: #6c757d;
            font-size: 18px;
        }
        
        .password-toggle {
            position: absolute;
            right: 15px;
            top: 50%;
            transform: translateY(-50%);
            color: #6c757d;
            cursor: pointer;
            font-size: 14px;
            font-weight: 500;
        }
        
        .remember-forgot {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }
        
        .remember-me {
            display: flex;
            align-items: center;
        }
        
        .remember-me input {
            margin-right: 8px;
        }
        
        .remember-me label {
            color: #6c757d;
            font-size: 14px;
        }
        
        .forgot-password a {
            color: #0e53ab;
            text-decoration: none;
            font-size: 14px;
            font-weight: 500;
        }
        
        .forgot-password a:hover {
            color: #0a3d7a;
            text-decoration: underline;
        }
        
        .btn-login {
            background: #0e53ab;
            color: white;
            border: none;
            border-radius: 8px;
            padding: 15px;
            font-size: 16px;
            font-weight: 500;
            cursor: pointer;
            transition: all 0.3s;
            margin-bottom: 20px;
        }
        
        .btn-login:hover {
            background: #0a3d7a;
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(14, 83, 171, 0.2);
        }
        
        .social-login {
            text-align: center;
            margin-top: 20px;
        }
        
        .social-login p {
            color: #6c757d;
            font-size: 14px;
            margin-bottom: 15px;
            position: relative;
        }
        
        .social-login p::before,
        .social-login p::after {
            content: "";
            position: absolute;
            top: 50%;
            width: 30%;
            height: 1px;
            background: #dee2e6;
        }
        
        .social-login p::before {
            left: 0;
        }
        
        .social-login p::after {
            right: 0;
        }
        
        .social-buttons {
            display: flex;
            gap: 10px;
            justify-content: center;
        }
        
        .social-btn {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 18px;
            transition: all 0.3s;
        }
        
        .social-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }
        
        .facebook {
            background: #3b5998;
        }
        
        .instagram {
            background: linear-gradient(45deg, #405DE6, #5851DB, #833AB4, #C13584, #E1306C, #FD1D1D);
        }
        
        .register-link {
            text-align: center;
            margin-top: 20px;
            color: #6c757d;
            font-size: 14px;
        }
        
        .register-link a {
            color: #0e53ab;
            text-decoration: none;
            font-weight: 600;
        }
        
        .register-link a:hover {
            text-decoration: underline;
        }
        
        .alert {
            background: #f8d7da;
            color: #721c24;
            padding: 15px;
            border-radius: 8px;
            margin-bottom: 20px;
            font-size: 14px;
            border: 1px solid #f5c6cb;
        }
        
        @media (max-width: 768px) {
            .login-container {
                flex-direction: column;
                width: 90%;
                height: auto;
            }
            
            .login-image {
                padding: 30px;
            }
            
            .login-form {
                padding: 40px 30px;
            }
        }
        
        .face-recognition {
            margin-bottom: 25px;
            text-align: center;
        }
        
        .face-recognition-label {
            display: block;
            margin-bottom: 10px;
            color: #6c757d;
            font-size: 14px;
        }
        
        .face-recognition input[type="file"] {
            display: none;
        }
        
        .face-upload-btn {
            background: #f8f9fa;
            border: 2px dashed #dee2e6;
            border-radius: 8px;
            padding: 15px;
            cursor: pointer;
            color: #6c757d;
            transition: all 0.3s;
            width: 100%;
            display: inline-block;
        }
        
        .face-upload-btn:hover {
            border-color: #0e53ab;
            color: #0e53ab;
        }
        
        .face-upload-btn i {
            margin-right: 8px;
        }
        
        .preview-image {
            max-width: 200px;
            max-height: 200px;
            margin-top: 10px;
            border-radius: 8px;
            display: none;
        }
    </style>
{% endblock %}

{% block body %}
<div class="login-container">
    <div class="login-image">
        <h2>Bienvenue</h2>
        <p>NextGenRh</p>
    </div>
    
    <div class="login-form">
        <h1>Connexion</h1>
        <p>Entrez vos identifiants pour accéder à votre compte</p>
        
        {% if error %}
            <div class="alert">
                {{ error.messageKey|trans(error.messageData, 'security') }}
            </div>
        {% endif %}

        {% for message in app.flashes('success') %}
            <div class="alert alert-success">
                {{ message }}
            </div>
        {% endfor %}

        {% for message in app.flashes('error') %}
            <div class="alert alert-danger">
                {{ message }}
            </div>
        {% endfor %}
        
        <form method="post" enctype="multipart/form-data">
            <div class="form-group">
                <i class="fas fa-envelope"></i>
                <input type="email" name="emailUser" value="{{ last_username }}" required placeholder="Adresse email" autofocus>
            </div>
            
            <div class="form-group">
                <i class="fas fa-lock"></i>
                <input type="password" name="password" class="pass-key" required placeholder="Mot de passe">
                <span class="password-toggle">Afficher</span>
            </div>
            
            <div class="remember-forgot">
                
                <div class="forgot-password">
                    <a href="#">Mot de passe oublié ?</a>
                </div>
            </div>
            
            <button type="submit" class="btn-login">Se connecter</button>
            
            <div class="mt-4 text-center">
                <p class="mb-3">- OU -</p>
                <div style="display: flex; justify-content: center; gap: 15px; align-items: center;">
                    <a href="{{ path('app_login_face') }}" style="display: inline-block;">
                        <span style="display: inline-flex; align-items: center; justify-content: center; width: 60px; height: 60px; border-radius: 50%; background: #0e53ab;">
                            <img src="/Login/images/OIP.png" alt="Connexion par reconnaissance faciale" style="height: 35px; width: 35px; border-radius: 50%; background: none; display: block;">
                        </span>
                    </a>
                    <a href="{{ path('connect_google') }}" style="display: inline-block;">
                        <span style="display: inline-flex; align-items: center; justify-content: center; width: 60px; height: 60px; border-radius: 50%; background: #ffffff; border: 2px solid #0e53ab;">
                            <img src="{{ asset('Login/images/googleicon.png') }}" alt="Google Icon" style="height: 35px; width: 35px; display: block;">
                        </span>
                    </a>
                </div>
            </div>
            
            <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
        </form>
        
        <div style="text-align:center; margin-top: 10px;">
            <a href="{{ path('app_login_face') }}" style="display: inline-block;">
                <span style="display: inline-flex; align-items: center; justify-content: center; width: 110px; height: 110px; border-radius: 50%; background: #0e53ab;">
                    <img src="/Login/images/OIP.png" alt="Connexion par reconnaissance faciale" style="height: 70px; width: 70px; border-radius: 50%; background: none; display: block;">
                </span>
            </a>
        </div>
        
        <div class="register-link">
            Pas encore de compte? <a href="{{ path('app_register') }}">S'inscrire</a>
        </div>
    </div>
</div>

<script>
    const pass_field = document.querySelector('.pass-key');
    const showBtn = document.querySelector('.password-toggle');
    showBtn.addEventListener('click', function(){
        if(pass_field.type === "password"){
            pass_field.type = "text";
            showBtn.textContent = "Masquer";
            showBtn.style.color = "#0e53ab";
        }else{
            pass_field.type = "password";
            showBtn.textContent = "Afficher";
            showBtn.style.color = "#6c757d";
        }
    });

    function previewImage(input) {
        const preview = document.getElementById('preview');
        const label = input.parentElement.querySelector('span');
        
        if (input.files && input.files[0]) {
            const reader = new FileReader();
            
            reader.onload = function(e) {
                preview.src = e.target.result;
                preview.style.display = 'block';
                label.textContent = input.files[0].name;
            }
            
            reader.readAsDataURL(input.files[0]);
        }
    }

    document.querySelector('input[name="face_image"]').addEventListener('change', function() {
        if (this.files.length > 0) {
            document.querySelector('input[name="password"]').removeAttribute('required');
        } else {
            document.querySelector('input[name="password"]').setAttribute('required', 'required');
        }
    });
</script>
{% endblock %} 