    <nav class="navbar col-lg-12 col-12 p-0 fixed-top d-flex flex-row">
      <div class="text-center navbar-brand-wrapper d-flex align-items-center justify-content-center">
        <a class="navbar-brand brand-logo mr-5" href="{{ path('app_back_office') }}">
          <img src="{{ asset('FrontOffice/images/logo.svg') }}" class="mr-2" alt="logo"/>
        </a>
        <a class="navbar-brand brand-logo-mini" href="{{ path('app_back_office') }}">
          <img src="{{ asset('FrontOffice/images/logo-mini.svg') }}" alt="logo"/>
        </a>
      </div>
      <div class="navbar-menu-wrapper d-flex align-items-center justify-content-end">


     <ul class="d-flex align-items-centre list-unstyled mb-0" style="gap:20px">
      <li>
                <div class="icon">
                    <i class="flaticon-email"></i>
                </div>
                <div class="info">
                    <span>Email: </span> nextgenrh@gmail.com
                </div>
            </li>
            
            <!-- Téléphone -->
            <li>
                <div class="icon">
                    <i class="flaticon-call"></i>
                </div>
                <div class="info">
                    <span>Phone:</span> +216 75 888 777
                </div>
            </li>
        
        </ul>
        <ul class="navbar-nav navbar-nav-right">
          
          <li class="nav-item nav-profile dropdown">
            <a class="nav-link dropdown-toggle" href="#" data-toggle="dropdown" id="profileDropdown">
              {% if app.user %}
                <span class="profile-text">
                  {% if app.user.prenomUser and app.user.nomUser %}
                    {{ (app.user.prenomUser|first ~ app.user.nomUser|first)|upper }}
                  {% elseif app.user.prenomUser %}
                    {{ (app.user.prenomUser|first)|upper }}
                  {% else %}
                    U
                  {% endif %}
                </span>
              {% else %}
                <img src="{{ asset('FrontOffice/images/faces/face28.jpg') }}" alt="profile"/>
              {% endif %}
            </a>
            <div class="dropdown-menu dropdown-menu-right navbar-dropdown" aria-labelledby="profileDropdown">
              {% if is_granted('ROLE_ResponsableRH') %}
                <a class="dropdown-item" href="{{ path('app_back_settings') }}">
                  <i class="ti-settings text-primary"></i>
                  Edit Profile
                </a>
              {% elseif is_granted('ROLE_Employe') %}
                <a class="dropdown-item" href="{{ path('app_front_settings') }}">
                  <i class="ti-settings text-primary"></i>
                  Edit Profile
                </a>
              {% endif %}
              <a class="dropdown-item" href="{{ path('app_logout') }}">
                <i class="ti-power-off text-primary"></i>
                Logout
              </a>
            </div>
          </li>
      
        </ul>
        
      </div>
    </nav>